# Backend Architecture (Microservices + API Gateway + Eureka)

## 1. Overview

This system uses a **microservices architecture** with:

- API Gateway (single entry point)
- Service Registry (Eureka)
- Multiple services:
    - Auth Service
    - Personnel Service
    - Inventory Service
    - Finance Service

---

